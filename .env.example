GENFARMER_BASE_URL=http://127.0.0.1:PORT
XPROXY_HOST=XPROXY_LAN_IP
DEFAULT_DEVICE_ADB=DEVICE_IP:5555
EVIDENCE_DIR=./evidence
LOG_LEVEL=INFO

# Never commit secrets. Add key NAMES only when new credentials are required.

# Optional constrained AI recovery advisor (never commit the real key).
MODCON_API_KEY=
MODCON_BASE_URL=https://modcon.top/v1
MODCON_MODEL=gpt-5.6-sol
