{
  "project": "TikTok Automation / GenFarmer",
  "visibility": "public-source-private-client-config",
  "phase": "tiktok-warmup-resilience-qualification",
  "verified_capabilities": [
    "20 Android devices visible simultaneously to ADB",
    "safe Device #1 Settings smoke workflow passed with local evidence",
    "GenFarmer API reachable and identified as 2.6.1",
    "official GenFarmer Local API documentation located",
    "fail-closed documented-API Python client implemented",
    "lossless script.flow reader/editor, round-trip tooling and local-only TemplateRegistry implemented",
    "renderer AST source discovery recovered 60 exact label/action/icon registry rows",
    "all 59 standalone palette action-node rows plus Start, Variables and ContextMenu are represented by exact saved live templates",
    "full live settings schema matrix executed for all 62 captured actions",
    "Chrome qualification proved GenFarmer StartApp/ADB/Pause/Screenshot/StopApp/Stop routing on Device #1",
    "TikTok package on Device #1 qualified as com.zhiliaoapp.musically",
    "TikTok version qualified as 46.7.3 (versionCode 2024607030)",
    "TikTok launcher qualified as com.zhiliaoapp.musically/com.ss.android.ugc.aweme.splash.SplashActivity",
    "GF Lab - TikTok Warmup Qualification base lifecycle executed successfully on Device #1",
    "a real exported .genfarm app was confirmed to be ordinary JSON containing complete app metadata plus script.flow, node options, positions and edges",
    "lossless .genfarm file helpers and a privacy-conscious local export inspector are implemented",
    "offline Python TikTok warm-up compiler implemented",
    "the default Swipe/Scroll node was captured as Simple/Up and its exact serialized options were extracted locally",
    "the compiler-generated READY warm-up contains 14 nodes, 11 edges and a validated Start -> StartApp -> Pause -> Swipe -> Pause -> Swipe -> Pause -> Swipe -> Pause -> Screenshot -> StopApp -> Stop route",
    "the compiled READY warm-up passed local graph validation with zero warnings",
    "the compiled READY flow was PUT into the intended live GenFarmer app using the preserved app identity",
    "post-PUT live verification found 14 nodes and 11 edges with runtime-semantic match YES",
    "GenFarmer normalization was limited to non-runtime editor position type differences in the observed verification sample",
    "the first live compiled warm-up run visibly executed on Device #1",
    "a TikTok Android notification-permission dialog was observed during that run, proving that node execution alone can produce false success when an overlay intercepts app actions",
    "a read-only ADB observer now classifies foreground package/activity and known coarse interrupt packages without mutating the device",
    "a bounded observe-act-verify-recover supervisor now exists with explicit preconditions, postconditions, local retry budget, recovery budget and hard-deadline support",
    "the combined resilience/runtime unit suite passed on the Windows lab: 13 passed",
    "the live resilience probe correctly classified Device #1 on the launcher as NOT_READY_FOREGROUND with no interrupt",
    "a bounded ADB recovery action helper exists for explicit authorized component relaunch only",
    "TikTok foreground planning distinguishes ready, needs-launch, blocked-interrupt and device-unavailable states",
    "tiktok_ensure_foreground.py dry-run correctly planned needs_launch while Device #1 was on the launcher",
    "tiktok_ensure_foreground.py --apply performed exactly one qualified-component relaunch and then proved TikTok foreground with no interrupt: PASS",
    "the high-level AutomationController exposes coarse observation, TikTok foreground recovery and verified-module supervision primitives",
    "standard-library raw Android screencap parsing and conservative temporal screen-stability measurement are implemented for secondary postcondition research",
    "a read-only tiktok_feed_stability_probe.py now measures how much of the current TikTok screen remains visually stable across a short animated window without tapping or swiping",
    "GenFarmer fingerprint rotation previously proven",
    "XProxy service and first-position listeners are reachable"
  ],
  "operator_reported_capabilities": [
    "TikTok is installed and available on Device #1",
    "the notification permission prompt was manually answered on Device #1 and may not recur on that device unless app/permission state changes",
    "one XProxy position is live/working; independent end-to-end proxy qualification is still pending"
  ],
  "current_findings": [
    "account creation/login/reset mode is explicitly out of scope for the current lane",
    "the active lane is TikTok warm-up plus boost qualification on one authorized test device/account",
    "the preferred architecture is compiler-style verified GenFarmer node templates plus Python graph assembly, with Python supervising short modules rather than trusting one long linear graph",
    "GenFarmer may normalize non-runtime editor fields after PUT, so deployment verification compares runtime semantics rather than requiring byte-for-byte flow identity",
    "the live warm-up graph currently matches compiled node actions, options, routing, source/target edges and handles",
    "a successful GenFarmer node log is not sufficient evidence that the intended TikTok state changed",
    "all meaningful modules must use observe -> interrupt classification -> precondition -> action -> postcondition -> checkpoint/recovery",
    "coarse foreground recovery is now live-qualified on Device #1 and can restore TikTok from the launcher without blind UI interaction",
    "the notification prompt may no longer be reproducible on Device #1, but the fail-closed interrupt classifier must remain because other devices or reset app state can surface permission dialogs later",
    "unknown or challenge states must fail closed; no blind taps or captcha/checkpoint bypass are allowed",
    "screenshot-derived evidence is being evaluated only as a secondary signal because animated video can create false positives; selector/accessibility evidence remains preferred",
    "production selectors must prefer resource-id/accessibility/content-desc/text/class/relative-XPath and must never rely on visually guessed coordinates",
    "standalone Android uiautomator dump is killed with exit 137 in the active GenFarmer/device session, while ADB itself remains healthy",
    "native selector qualification will therefore use GenFarmer's own inspector/selector capabilities first",
    "the client requirements include saveable warm-up/boost presets, bounded sessions, exploration, posting, logging and scheduler/barrier behavior",
    "client-prohibited actions include DMs, mass follow/unfollow, cross-account engagement, proxy rotation during login/upload and captcha/checkpoint bypass",
    "public source must not contain client IPs, credentials, raw selectors tied to private accounts, proxy URLs or account identifiers"
  ],
  "blockers": [
    "selector-level feed precondition and postcondition checks are not yet captured, so browse_one cannot yet prove that a swipe changed the intended TikTok feed state",
    "the visual stability characteristics of Device #1 TikTok feed still need to be measured before deciding whether image evidence is useful as a secondary postcondition signal",
    "stable native TikTok selectors for Home/FYP, search/discover, profile and content containers are not yet captured for richer passive warm-up modules",
    "the Android notification permission handler remains unqualified for automatic dismissal; until needed it must continue to fail closed rather than guess",
    "boost explore/publish flow has not yet been qualified",
    "failure/branch edge semantics remain incomplete",
    "the reported live XProxy position still needs an end-to-end HTTP(S) egress check before proxy-required workflows are enabled"
  ],
  "next": [
    "run the new screen-state unit tests",
    "while TikTok is foreground on the feed, run scripts/tiktok_feed_stability_probe.py and record the stable-screen ratio",
    "use that measurement to decide whether a conservative before/after visual comparator is worth adding as secondary evidence or whether browse_one should wait exclusively for selector/accessibility postconditions",
    "capture a stable feed anchor in GenFarmer's native inspector and save the exact selector configuration privately",
    "implement browse_one as a short supervised module with foreground/interrupt precondition, one GenFarmer Swipe, and a fail-closed postcondition",
    "qualify repeated browse_one cycles under the Python supervisor with bounded retries and evidence",
    "qualify XProxy end-to-end before enabling any proxy-required TikTok workflow",
    "after resilience gates are green, extend passive warm-up with profile/comment/niche-search modules"
  ]
}
